window.SIPIC_CONFIG = Object.freeze({
  // O servidor local redireciona /api para a Edge Function do Supabase.
  apiBaseUrl: "/api",
  requestTimeoutMs: 18000,
  automaticRefreshMs: 10 * 60 * 1000,
  analyticsHours: 168,
});
